def water(x):  
    m = x * x  # The variable `m` is unnecessary. You can directly return the result of the calculation.  
    return m**0.5  # Consider renaming the function and variable to improve clarity.
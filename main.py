from first import sqr  # Importing the `sqr` function from the `first` module, presumably for squaring a number.
from second import special  # Importing the `special` function from the `second` module, purpose unknown without further context.

a = 10  # Assigning the value 10 to the variable `a`.

print(sqr(a))  # Calling the `sqr` function with `a` and printing the result.
print(special(a))  # Calling the `special` function with `a` and printing the result.
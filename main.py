from first import div  # Importing the `div` function from the `first` module. Ensure the module exists and the function is defined.
from second import special  # Importing the `special` function from the `second` module. Ensure the module exists and the function is defined.

a = 10  # Defining a variable `a` with a value of 10. Consider using a more descriptive name.

print(div(a))  # Calling the `div` function with `a` as an argument. Ensure `div` accepts one argument and is implemented correctly.
print(special(a))  # Calling the `special` function with `a` as an argument. Ensure `special` accepts one argument and is implemented correctly.
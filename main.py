from a import SQr  # Importing the `SQr` function from module `a`. Ensure the module and function exist and are correctly implemented.
a = 10  # Assigning the value 10 to the variable `a`. Consider using a more descriptive variable name.
print(SQr(a))  # Calling the `SQr` function with `a` as an argument and printing the result. Add error handling to manage potential issues.
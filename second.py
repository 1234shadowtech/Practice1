from first import sq, sqr  # Importing the functions `sq` and `sqr` from the module `first`

def special(x):  
    n = (sq(x) - sqr(x)) * 4  # Calculating the difference of `sq(x)` and `sqr(x)`, multiplying by 4, and assigning it to `n` (unused)
    return (sq(x) - sqr(x)) * 4  # Returning the same calculation as above
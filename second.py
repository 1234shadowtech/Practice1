from first import sq, sqr  # Importing `sq` and `sqr` functions from the `first` module. Ensure these functions are defined and implemented correctly.

def special(x):  # Defining the `special` function that takes one argument `x`.
    return (sq(x) - sqr(x)) * 2  # Subtracting the result of `sqr(x)` from `sq(x)` and multiplying the result by 2. Ensure `sq` and `sqr` return compatible numeric types.
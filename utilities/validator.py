def is_positive(number):  
    return number > 0  # Checks if the number is greater than 0 and returns True if it is, otherwise False.  

def is_even(number):  
    return number % 2 == 0  # Checks if the number is even by verifying if the remainder when divided by 2 is 0. Returns True for even numbers, otherwise False.